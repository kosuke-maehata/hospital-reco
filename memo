#リポジトリ構成

HospitalApp/
├─ .venv/               ← (これから作成する) Python 仮想環境
├─ app.py               ← Streamlit アプリ本体
├─ hospitals.json       ← 病院データ
├─ requirements.txt     ← 必要な Python パッケージ一覧
└─ start_app.bat        ← （これをダブルクリックで起動）



#仮想環境の作成

# 1) フォルダへ移動
cd path\to\HospitalApp

# 2) 仮想環境の作成
python -m venv .venv

# 3) 仮想環境をアクティベート
.venv\Scripts\Activate.ps1

# 4) 必要パッケージをインストール
pip install -r requirements.txt

# 5) 仮想環境を離脱（不要ならスキップ）
deactivate
